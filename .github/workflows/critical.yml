name: critical CSS

on:
  push:
    branches:
    
jobs:
  crittr:
      runs-on: ubuntu-latest
      steps:
      - name: getcss
        run: |
          npm i crittr
          node contrast_theme_node.js