name: critical CSS

on:
  push:
    branches:
    
jobs:
  crittr:
      runs-on: ubuntu-latest
      steps:
      - name: getcss
        run: |
          npm i crittr
          ls -a
          cat contrast_theme_node.js
          node contrast_theme_node.js