name: critical CSS

on:
  push:
    branches:
    
jobs:
  crittr:
      name: crittr
      runs-on: ubuntu-latest
      steps:
        run: |
          npm i crittr
          node contrast_theme_node.js